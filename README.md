# live-chat

## 시작하기

```shell
# pnpm을 설치하지 않았다면 설치합니다.
# - npm으로 설치: npm install -g pnpm 또는
# - corepack 활성화: corepack enable

# 의존성을 설치합니다.
pnpm install
# 서버와 클라이언트를 동시에 실행합니다.
pnpm start

# 서버와 클라이언트를 개별적으로 실행하고 싶다면,
# 서버만
pnpm server:start
# 클라이언트만
pnpm client:start
```
